name: flutter_hack20
description: flutter hack20 app

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.7.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  provider: ^4.1.3

  # firebase
  cloud_firestore: ^0.13.7
  firebase_auth: ^0.16.1

flutter:
  uses-material-design: true
